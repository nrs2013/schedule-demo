# R186-R187 動作確認チェックリスト

> 2026-05-15 セッション追加分。GAS デプロイ完了後、のむさんがスマホ＋PC で確認する。

## 概要

LINE グループでもリッチメニュー風の操作を実現するため、以下を追加：

- **R186**：Bot がグループ参加時に「6 ボタン Flex Message」を自動送信。グループで「メニュー」と打っても再送される
- **R187**：pc-edit の LINE 連携セクションに「📋 メニューカード送信」ボタン追加（GAS の `pushMenu` action）

## 動作確認手順

### Phase 1：GAS デプロイ確認

- [ ] GAS エディタで R186-R187 のコードに貼り替え
- [ ] **24 行目の `CHANNEL_ACCESS_TOKEN` を本物のトークンに書き戻し**（重要）
- [ ] 「デプロイを管理」→ 既存デプロイを編集 → 「新しいバージョン」でデプロイ
- [ ] Webhook URL は変わらないことを確認（v2 のままで OK）

### Phase 2：新規グループに Bot 招待テスト

- [ ] LINE スマホで「テスト用グループ」を作成（既存「テスト」とは別の名前で）
- [ ] 「SCHEDULE STUDIO」を友だちから選んで招待
- [ ] グループにメッセージが順番に届く：
  - [ ] テキスト：「SCHEDULE STUDIO Bot が参加しました…」
  - [ ] **6 ボタンカード（TODAY / CALENDAR / REHEARSAL / INFO / HOTEL / PASS）**
- [ ] カードのボタンを 1 つ押す → ブラウザで該当タブが開くか

### Phase 3：「メニュー」キーワード再送テスト

- [ ] グループ内で「**メニュー**」と入力 → Bot がカード送信
- [ ] 「menu」「めにゅー」でも同じカードが送られる
- [ ] その他の単語（「おはよう」など）には反応しない

### Phase 4：アナウンス（ピン留め）動作確認

- [ ] 届いた 6 ボタンカードを **長押し**
- [ ] メニューから「**アナウンス**」を選択
- [ ] グループ画面上部に黄色いバーで「SCHEDULE STUDIO メニュー」が常時表示される
- [ ] 会話を進めても、上部バーが消えないことを確認
- [ ] バーをタップ → カードまでスクロールジャンプ

### Phase 5：pc-edit からのメニュー再送

- [ ] PC で pc-edit を開く
- [ ] INFO タブ → LINE 連携セクション
- [ ] 「**📋 メニューカード送信**」ボタン（緑色）が表示されている
- [ ] 現在の公演に LINE グループ ID を紐付けた状態でボタンを押す
- [ ] 「✓ メニューカードを送信しました」と表示される
- [ ] 該当グループに 6 ボタンカードが届く

### Phase 6：既存機能の回帰確認（壊れてないか）

- [ ] 「テスト送信」ボタンで従来通り text 通知が届く
- [ ] phone-staff のリッチメニュー hash routing（R185）が動く
- [ ] CDS など他機能に影響なし

## トラブルシューティング

| 症状 | 原因 | 対処 |
|---|---|---|
| 招待してもメッセージが来ない | GAS のデプロイがバージョン更新じゃなく新規になっている | デプロイを管理 → 既存デプロイの編集 → バージョン更新 |
| カードは来るがボタンタップで何も起きない | hash routing の URL が間違ってる | line-gas-bot.gs の `buildMenuFlex` 内 base URL を確認 |
| 「✗ 失敗: …」と pc-edit に出る | GAS URL が古い、または CORS | URL を保存し直し、`Content-Type: text/plain` 確認 |
| 「メニュー」と打っても反応なし | text マッチが失敗 | 半角スペース混入なし、`メニュー`の完全一致を確認 |
| カードのボタン色が見えづらい | 暗い環境で白文字 | 色は #D85A30 など濃色ベース、白文字。問題あればテーマ調整 |

## 完了確認

すべてチェックが付いたら、引き継ぎ書を R187 完了で更新する（または次セッション開始時に Claude が更新）。
